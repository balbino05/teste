{
    "scripts": {
        "generate-docs": "swagger-jsdoc -d swaggerDef.js -o swagger.json",
        "test": "mocha --exit",
  "test-watch": "mocha --watch --exit"
    },
    "devDependencies": {
        "chai": "^5.0.3",
        "mocha": "^10.2.0",
        "supertest": "^6.3.4"
    }
}
